name: Build AlwaysEnglish

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-2022
    
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: 'recursive'
    
    - name: Install Qt 6.6.3 using Aqtinstall
      shell: cmd
      run: |
        pip install aqtinstall
        aqt install-qt windows desktop 6.6.3 win64_msvc2019_64 --outputdir D:\Qt
    
    - name: Setup MSVC
      uses: ilammy/msvc-dev-cmd@v1
    
    - name: Configure CMake
      shell: cmd
      run: |
        set PATH=D:\Qt\6.6.3\msvc2019_64\bin;%PATH%
        mkdir build
        cd build
        cmake .. -DCMAKE_PREFIX_PATH=D:\Qt\6.6.3\msvc2019_64 -DCMAKE_BUILD_TYPE=Release -GNinja -DFLUENTUI_QML_PLUGIN_DIRECTORY=%cd%\..\bin\Release\qml\FluentUI
    
    - name: Build
      shell: cmd
      run: |
        set PATH=D:\Qt\6.6.3\msvc2019_64\bin;%PATH%
        cd build
        ninja
    
    - name: Show output directories
      shell: cmd
      run: |
        dir bin\Release\
    
    - name: Run windeployqt
      shell: cmd
      run: |
        set PATH=D:\Qt\6.6.3\msvc2019_64\bin;%PATH%
        windeployqt bin\Release\AlwaysEnglish.exe --qmldir=src\qml --no-translations --compiler-runtime
    
    - name: Create dist directory
      shell: cmd
      run: |
        mkdir dist
        xcopy bin\Release\ dist\ /E /H /C /I
    
    - name: List dist contents
      shell: cmd
      run: |
        dir dist\
        dir dist\qml\
        dir dist\plugins\
    
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: AlwaysEnglish-Win64
        path: dist/